{
    "name": "css-html-practice",
    "version": "1.0.0",
    "description": "CSS and HTML practice course",
    "main": "index.html",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build:css": "npm run css:lint && npm run css:autoprefixer",
        "serve": "browser-sync start --browser chrome --server \"static\" --files \"static\""
    },
    "author": "Michal Turczyn",
    "devDependencies": {
        "browser-sync": "^2.29.3"
    },
    "dependencies": {
        "idb": "^8.0.0"
    }
}
